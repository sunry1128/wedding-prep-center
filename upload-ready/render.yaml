services:
  - type: web
    name: wedding-prep-center
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATA_PATH
        value: /var/data/db.json
    disk:
      name: wedding-data
      mountPath: /var/data
      sizeGB: 1
